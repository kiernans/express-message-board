extends layout

block content
  a(href='/new') Create New Message
  h1= title
  p Welcome to #{title}
  each message in messages.length ? messages : ['There are no messages'] 
    p #{message.user}
    p #{message.text}
    p #{message.added}
